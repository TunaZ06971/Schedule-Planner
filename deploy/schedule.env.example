# Copy this file to /etc/schedule.env on the server.
# Keep the real file out of git and lock it down with chmod 600.

WRITE_TOKEN=replace-with-a-long-random-token

# Optional. Used for stable UID host names in exported calendar events.
ICS_HOSTNAME=schedule.cks06971.com
